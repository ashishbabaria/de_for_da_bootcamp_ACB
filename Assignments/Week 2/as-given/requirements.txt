# Session uses only the Python standard library.
# There is nothing to install. The file is here to show the habit:
# in a real project you would list your packages, one per line, e.g.
#   requests
#   pandas
# and install them with:  pip install -r requirements.txt
